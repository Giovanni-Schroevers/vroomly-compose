query login($email: String!, $password: String!) {
    login(input: {email: $email, password: $password}) {
        token
        user {
            id
            firstname
            middleName
            lastname
            email
            dateOfBirth
            username
        }
    }
}