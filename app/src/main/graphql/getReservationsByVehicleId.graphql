query GetReservationsByVehicleId($vehicleId: Int!) {
    getReservationsByVehicleId(vehicleId: $vehicleId) {
        id
        startDate
        endDate
        status
        totalCost
        renterId
        paid
        createdAt
        vehicleId
    }
}
