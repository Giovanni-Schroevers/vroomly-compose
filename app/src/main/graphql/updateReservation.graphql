query UpdateReservation($input: ReservationUpdateInput!) {
  updateReservation(input: $input) {
    id
    startDate
    endDate
    status
    totalCost
    paid
    createdAt
    vehicleId
    renterId
  }
}