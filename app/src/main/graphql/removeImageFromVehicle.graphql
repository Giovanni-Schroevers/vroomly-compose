mutation RemoveImageFromVehicle($vehicleId: Int!, $imageId: Int!) {
    removeImageFromVehicle(vehicleId: $vehicleId, imageId: $imageId) {
        id
        images {
            id
            number
            url
        }
    }
}
