query DeleteReservation($reservationId: Int!) {
  deleteReservation(reservationId: $reservationId) {
    id
    startDate
    endDate
    status
    totalCost
    paid
    createdAt
    vehicleId
    renterId
  }
}