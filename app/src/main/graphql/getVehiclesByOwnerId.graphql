query GetVehiclesByOwnerId($ownerId: Int!) {
    getVehiclesByOwnerId(ownerId: $ownerId) {
        id
        brand
        model
        costPerDay
        engineType
        ownerId
        reviewStars
        location {
            address
        }
        images {
            url
            number
        }
    }
}
