query GetVehicleById($vehicleId: Int!) {
  getVehicleById(vehicleId: $vehicleId) {
    id
    images {
      url
      number
    }
    location {
      longitude
      latitude
      address
    }
  }
}
