query GetReservationsByRenterId($renterId: Int!) {
  getReservationsByRenterId(renterId: $renterId) {
    id
    startDate
    endDate
    status
    totalCost
    paid
    createdAt
    vehicleId
    renterId
  }
}